---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: aitrade
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: aitrade
  policy:
    semver:
      range: ">=0.0.0"
