apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - gitrepository.yaml
  - aitrade-prod-kustomization.yaml
  - image-repository.yaml
  - image-policy.yaml
  - image-automation.yaml

# TODO before resuming aitrade-flux:
# 1. Copy clusters/homelab/secrets/aitrade-auth.sops.yaml.example to
#    clusters/homelab/secrets/aitrade-auth.sops.yaml.
# 2. Replace placeholders with the real ai-trade deploy key Secret.
# 3. Encrypt with: sops --encrypt --in-place clusters/homelab/secrets/aitrade-auth.sops.yaml
# 4. Add the encrypted file to this resources list:
#      - ../secrets/aitrade-auth.sops.yaml
#
# The real encrypted Secret is intentionally not referenced while it does not
# exist, so repo-only `kubectl kustomize` validation remains green.
