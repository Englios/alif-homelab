**/data

**/ignore*
.env
**.secret.*
**/*secret.*
!**/*secret.example.*
/.secret*
*.ignore

**/gpu-feature-discovery

# --- Secret / key exclusions ---
*.agekey
age.key
*.dec.yaml
*.decrypted.yaml
old-flux-system-secret.yaml
aitrade-deploykey*

# Keep encrypted SOPS files trackable via version control
!**/*.sops.yaml
!**/*.enc.yaml

kubeconfig
*.kubeconfig
.jj/

# BEGIN oh-my-opencode-slim worktrees + deepwork
# Worktree state files stay local; deepwork drafts ARE tracked in git so
# the planning state for upcoming work is versioned alongside the repo.
.slim/*
.slim/deepwork/
.slim/deepwork/**
# END oh-my-opencode-slim worktrees + deepwork