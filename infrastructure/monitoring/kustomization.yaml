apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Monitoring scrapes for Flux + GPU workloads.
# Applied directly to the cluster for now; will be wired to a Flux
# Kustomization in the monitoring-stack migration (see
# docs/plans/monitoring-migration.md).
namespace: monitoring
resources:
  - podmonitor-flux.yaml
  - servicemonitor-dcgm.yaml
