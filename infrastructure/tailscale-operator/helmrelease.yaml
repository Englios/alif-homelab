---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tailscale-operator
  namespace: tailscale
spec:
  releaseName: tailscale-operator
  chart:
    spec:
      chart: tailscale-operator
      version: "1.98.4"
      sourceRef:
        kind: HelmRepository
        name: tailscale
        namespace: flux-system
  interval: 30m
  install:
    createNamespace: true
  upgrade:
    cleanupOnFail: false
  # The chart at https://pkgs.tailscale.com/helmcharts authenticates
  # via OAuth. The OAuth client credentials live in the encrypted
  # Secret/operator-oauth in the 'tailscale' namespace; their values
  # never appear in this file or in git.
  values:
    operatorConfig:
      hostname: tailscale-operator
  valuesFrom:
    - kind: Secret
      name: operator-oauth
      valuesKey: client_id
      targetPath: oauth.clientId
    - kind: Secret
      name: operator-oauth
      valuesKey: client_secret
      targetPath: oauth.clientSecret
